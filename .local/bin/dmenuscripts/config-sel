#!/bin/sh
dir="${HOME}"/.local/bin

### FOR DMENU ###
# Result=$(cd "${dir}"; find * -type f | dmenu)

### FOR ROFI ###
Result=$(cd "${dir}"; find * -type f | rofi -dmenu)

if [ -n "$Result" ]; then
	xterm -e ${SHELL} -c "vim ${dir}/'$Result'"
fi


