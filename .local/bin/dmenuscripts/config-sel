#!/bin/sh
dir="${HOME}"/.local/bin

### FOR DMENU ###
# Result=$(cd "${dir}"; find * -type f | dmenu)

### FOR ROFI ###
Result=$(cd "${dir}"; find * -type f | rofi -dmenu)

if [ -n "$Result" ]; then
	alacritty -e ${SHELL} -c "sleep 0.2; vim ${dir}/'$Result'"
fi


