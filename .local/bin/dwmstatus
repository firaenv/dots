#!/bin/sh

while true; do

         LOCALTIME=$(date '+%a %I:%M %P ')
		 VOL=$(pactl list sinks | tr ' ' '\n' | grep -m1 '%')
         # BATTERY=$(apm | awk '{ print substr( $4, 0 ); exit }') 

         xsetroot -name " VOL:$VOL | $LOCALTIME"

         sleep 6
done 
